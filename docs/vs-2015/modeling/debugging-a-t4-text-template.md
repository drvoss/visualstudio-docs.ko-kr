---
title: T4 텍스트 템플릿 디버그 | Microsoft Docs
ms.date: 11/15/2016
ms.prod: visual-studio-dev14
ms.technology: vs-ide-modeling
ms.topic: conceptual
helpviewer_keywords:
- text templates, troubleshooting
- text templates, debugging
ms.assetid: 0877fdf2-20bf-42da-b3cc-4c5856b80821
caps.latest.revision: 30
author: jillre
ms.author: jillfra
manager: jillfra
ms.openlocfilehash: a8054b97d35b2cd5ad1cc68a747448bc7ce4b0d8
ms.sourcegitcommit: a8e8f4bd5d508da34bbe9f2d4d9fa94da0539de0
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 10/19/2019
ms.locfileid: "72654923"
---
# <a name="debugging-a-t4-text-template"></a>T4 텍스트 템플릿 디버깅
[!INCLUDE[vs2017banner](../includes/vs2017banner.md)]

텍스트 템플릿에서 중단점을 설정할 수 있습니다. 디자인 타임 텍스트 템플릿을 디버깅 하려면 텍스트 템플릿 파일을 저장 한 다음 솔루션 탐색기 파일의 바로 가기 메뉴에서 **T4 템플릿 디버그** 를 선택 합니다. 런타임 텍스트 템플릿을 디버깅 하려면 자신이 속한 응용 프로그램을 디버그 하면 됩니다.

 텍스트 템플릿을 디버깅 하려면 템플릿 변환 프로세스의 단계를 이해 해야 합니다. 각 단계에서 다양 한 종류의 오류가 발생할 수 있습니다. 단계는 다음과 같습니다.

|단계|디자인 타임 템플릿: 발생 시기|런타임 템플릿: 발생 하는 경우|
|----------|--------------------------------------------|-----------------------------------------|
|코드는 텍스트 템플릿에서 생성 됩니다.<br /><br /> 지시문의 오류 이거나, 일치 하지 않거나 순서가 지정 된 `<#…#>` 태그입니다.|템플릿을 저장 하거나 텍스트 변환을 호출 하는 경우|템플릿을 저장 하거나 텍스트 변환을 호출 하는 경우|
|생성 된 코드가 컴파일됩니다.<br /><br /> 템플릿 코드에서 컴파일 오류가 발생 했습니다.|이전 단계 바로 다음에|응용 프로그램 코드와 함께.|
|코드 실행.<br /><br /> 템플릿 코드에서 런타임 오류가 발생 했습니다.|이전 단계 바로 다음에|응용 프로그램을 실행 하 고 템플릿 코드를 호출 하는 경우|

 대부분의 경우 템플릿 코드의 줄 번호는 오류 보고서에 제공 됩니다. 오류 보고서가 임시 파일 이름을 참조 하는 경우 일반적인 원인으로는 텍스트 템플릿의 코드에 일치 하지 않는 대괄호가 있습니다.

 텍스트 템플릿에서 중단점을 설정 하 고 일반적인 방법으로 디버그할 수 있습니다.

## <a name="common-errors-and-fixes"></a>일반적인 오류 및 수정
 다음 표에서는 가장 일반적인 오류 및 해결 방법을 보여 줍니다.

|오류 메시지|설명|솔루션|
|-------------------|-----------------|--------------|
|변환 클래스가 상속 하는 기본 클래스 ' {0} '을 (를) 로드 하지 못했습니다.|템플릿 지시문의 `inherits` 매개 변수에 지정 된 기본 클래스를 찾을 수 없는 경우 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|지정 된 클래스가 존재 하 고 해당 클래스가 있는 어셈블리가 어셈블리 지시문에 지정 되어 있어야 합니다.|
|파일에 대 한 포함 텍스트를 확인 하지 못했습니다. {0}|포함 된 템플릿을 찾을 수 없는 경우 발생 합니다. 이 메시지는 요청 된 포함 파일의 이름을 제공 합니다.|파일 경로가 원래 템플릿 경로에 상대적 이거나 파일이 호스트에 등록 된 위치에 있는지 또는 파일에 대 한 전체 경로가 있는지를 확인할 수 있습니다.|
|변환 개체를 초기화할 때 오류가 발생 했습니다. 변환이 실행 되지 않습니다.|변환 클래스의 ' Initialize () '가 실패 하거나 false를 반환할 때 발생 합니다.|Initialize () 함수의 코드는 \< # @template # > 지시문 및 지시문 프로세서에서 지정 된 기본 변환 클래스에서 제공 됩니다. 오류 목록에 초기화가 실패 한 것으로 나타나는 오류가 있을 수 있습니다. 실패 한 이유를 조사 합니다. 템플릿을 디버그 하는 절차에 따라 Initialize ()에 대해 생성 된 실제 코드를 살펴볼 수 있습니다.|
|' @No__t_1 ' 지시문 프로세서에 대 한 ' {0} ' 어셈블리에 FullTrust 권한 집합이 부여 되지 않았습니다. 신뢰할 수 있는 어셈블리만 지시문 프로세서를 제공할 수 있습니다. 이 지시문 프로세서는 로드 되지 않습니다.|시스템에서 지시문 프로세서를 포함 하는 어셈블리에 FullTrust 권한을 부여 하지 않을 때 발생 합니다. 메시지는 어셈블리의 이름과 지시문 프로세서의 이름을 제공 합니다.|로컬 컴퓨터에서 신뢰할 수 있는 어셈블리만 사용 해야 합니다.|
|' @No__t_0 ' 경로는이 컴퓨터에 로컬 이거나 신뢰할 수 있는 영역의 일부 여야 합니다.|지시문 또는 어셈블리 지시문이 로컬 컴퓨터 또는 네트워크의 신뢰할 수 있는 영역에 없는 파일을 참조할 때 발생 합니다.|지시문 또는 어셈블리 지시문이 있는 디렉터리는 신뢰할 수 있는 영역에 있어야 합니다. Internet Explorer를 통해 신뢰할 수 있는 영역에 네트워크 디렉터리를 추가할 수 있습니다.|
|"잘못 된 토큰 ' catch '" 또는 "네임 스페이스에는 멤버를 직접 포함할 수 없습니다."와 같은 여러 구문 오류가 있습니다.|템플릿 코드에 닫는 중괄호가 너무 많습니다. 컴파일러는 표준 생성 코드와 혼동 합니다.|코드 구분 기호 내에서 닫는 중괄호와 대괄호의 수를 확인 합니다.|
|루프 또는 조건이 올바르게 컴파일되지 않거나 실행 되지 않았습니다. 예를 들어 `<#if (i>10)#> Number is: <#= i #>`을 참조하십시오.<br /><br /> 이 코드는 항상 i 값을 출력 합니다. "숫자만:"은 조건입니다.|에서 C#항상 중괄호를 사용 하 여 제어 문에 포함 된 텍스트 블록을 묶습니다.|중괄호 추가: `<#if (i>10) { #>    Number is: <#= i #><# } #>`.|
|"식이 너무 복잡 합니다."는 디자인 타임 템플릿을 처리 하거나 런타임 (전처리) 템플릿을 컴파일할 때입니다.<br /><br /> 런타임 템플릿에서 생성 된 코드를 검사 하려고 하면 [!INCLUDE[vsprvs](../includes/vsprvs-md.md)] 작동이 중지 됩니다.|텍스트 블록이 너무 깁니다. T4는 각 템플릿 줄에 대해 하나의 문자열 리터럴을 사용 하 여 텍스트 블록을 문자열 연결 식으로 변환 합니다. 매우 긴 텍스트 블록은 컴파일러의 크기 제한을 overstep 수 있습니다.|다음과 같은 식 블록을 사용 하 여 긴 텍스트 블록을 분할 합니다.<br /><br /> `<#= "" #>`|

## <a name="warning-descriptions-and-fixes"></a>경고 설명 및 수정
 다음 표에서는 사용 가능한 경우 가장 자주 발생 하는 경고를 수정과 함께 보여 줍니다.

|경고 메시지|설명|솔루션|
|---------------------|-----------------|--------------|
|' @No__t_0 ' 포함 파일을 로드 하는 동안 null 또는 빈 문자열이 반환 되었습니다.|포함 된 텍스트 템플릿 파일이 비어 있는 경우에 발생 합니다. 메시지는 포함 된 파일의 파일 이름을 제공 합니다.|Include 지시문을 제거 하거나 파일에 일부 내용이 포함 되어 있어야 합니다.|
|변환 컴파일:|변환을 컴파일할 때 컴파일러에서 발생 하는 모든 오류 또는 경고에이 문자열을 앞에 표시 합니다. 이 문자열은 컴파일러가 오류 또는 경고를 throw 했음을 의미 합니다.|DLL을 찾는 데 문제가 있는 경우 DLL이 GAC에 있으면 전체 경로 또는 정규화 된 강력한 이름을 제공 해야 할 수 있습니다.|
|' @No__t_0 ' 매개 변수가 이미 지시문에 있습니다. 중복 된 매개 변수는 무시 됩니다.|매개 변수가 지시문에서 두 번 이상 지정 되 면 발생 합니다. 이 메시지는 매개 변수의 이름과 지시문의 줄 번호를 제공 합니다.|중복 매개 변수 사양을 제거 합니다.|
|포함 파일 ' {0} '을 (를) 로드 하는 동안 오류가 발생 했습니다. Include 지시문은 무시 됩니다.|@No__t_0 지시문에 지정 된 파일을 찾을 수 없는 경우 발생 합니다. 메시지는 파일의 이름과 지시문의 줄 번호를 제공 합니다.|포함 파일이 원래 텍스트 템플릿 파일과 동일한 디렉터리에 있거나 호스트에 등록 된 포함 디렉터리 중 하나에 있어야 합니다.|
|변형 클래스에 대해 잘못 된 기본 클래스를 지정 했습니다. 기본 클래스는 VisualStudio 변환에서 파생 되어야 합니다.|템플릿 지시문의 `inherits` 매개 변수가 `TextTransformation`에서 상속 되지 않는 클래스를 지정 하는 경우 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|@No__t_0에서 파생 되는 클래스를 지정 합니다.|
|' Template ' 지시문에 잘못 된 문화권이 지정 되었습니다. 문화권이 "xx-XX" 형식 이어야 합니다. 고정 문화권이 사용 됩니다.|템플릿 지시문의 culture 매개 변수가 잘못 지정 된 경우에 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|Culture 매개 변수를 "xx-XX" 형식의 유효한 문화권으로 변경 합니다.|
|템플릿 지시문에 잘못 된 디버그 값 ' {0} '이 (가) 지정 되었습니다. 디버그 값은 "true" 또는 "false" 중 하나 여야 합니다. 기본값인 "false"가 사용 됩니다.|템플릿 지시문의 `debug` 매개 변수가 잘못 지정 된 경우에 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|디버그 매개 변수를 "true" 또는 "false"로 설정 합니다.|
|템플릿 지시문에 잘못 된 HostSpecific 값 ' {0} '이 (가) 지정 되었습니다. HostSpecific 값은 "true" 또는 "false" 여야 합니다. 기본값인 "false"가 사용 됩니다.|@No__t_0 지시어의 호스트 관련 매개 변수가 잘못 지정 된 경우에 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|호스트 관련 매개 변수를 "true" 또는 "false"로 설정 합니다.|
|' Template ' 지시문에 잘못 된 언어 ' {0} '이 (가) 지정 되었습니다. 언어는 "C#" 또는 "VB" 중 하나 여야 합니다. "C#"의 기본값이 사용 됩니다.|@No__t_0 지시문에 지원 되지 않는 언어가 지정 된 경우 발생 합니다. "C#" 또는 "VB"만 허용 됩니다 (대/소문자 구분 안 함). 메시지는 템플릿 지시문의 줄 번호를 제공 합니다.|템플릿 지시문의 `language` 매개 변수를 "C#" 또는 "VB"로 설정 합니다.|
|템플릿에 여러 출력 지시문이 있습니다. 첫 번째 항목을 제외한 모든 항목은 무시 됩니다.|템플릿 파일에 여러 `output` 지시문이 지정 된 경우 발생 합니다. 메시지는 중복 출력 지시문의 줄 번호를 제공 합니다.|중복 `output` 지시문을 제거 합니다.|
|템플릿에 여러 템플릿 지시문이 있습니다. 첫 번째 항목을 제외한 모든 항목은 무시 됩니다. 템플릿 지시문에 대 한 여러 매개 변수는 하나의 템플릿 지시문 내에서 지정 해야 합니다.|포함 된 파일을 포함 하 여 텍스트 템플릿 파일 내에 여러 개의 `template` 지시문을 지정 하는 경우 발생 합니다. 이 메시지는 중복 된 템플릿 지시문의 줄 번호를 제공 합니다.|서로 다른 `template` 지시문을 하나의 `template` 지시문으로 집계 합니다.|
|' @No__t_0 ' 지시문에 대해 프로세서가 지정 되지 않았습니다. 지시문은 무시 됩니다.|@No__t_0 지시어를 지정 하지만 `processor` 특성을 제공 하지 않을 경우 발생 합니다. 이 메시지는 지시문의 이름과 줄 번호를 제공 합니다.|지시문에 대 한 `directive` 프로세서의 이름과 함께 `processor` 특성을 제공 합니다.|
|' @No__t_1 ' 지시문에 대 한 ' {0} ' (이) 라는 프로세서를 찾을 수 없습니다. 지시문은 무시 됩니다.|시스템에서 `custom` 지시문 내에 지정한 `directive` 프로세서를 찾을 수 없을 때 발생 합니다. 이 메시지는 지시문 이름, 프로세서 이름 및 지시문의 줄 번호를 제공 합니다.|지시문의 `processor` 특성을 지시문 프로세서의 이름으로 설정 합니다.|
|' @No__t_1 ' 지시문에 대 한 필수 매개 변수 ' {0} '이 (가) 없습니다. 지시문은 무시 됩니다.|시스템에서 필수 지시문 매개 변수를 제공 하지 않을 때 발생 합니다. 이 메시지는 누락 된 매개 변수의 이름, 지시문 이름 및 줄 번호를 제공 합니다.|누락 된 매개 변수를 제공 합니다.|
|' @No__t_0 ' (이) 라는 프로세서는 ' {1} ' 지시문을 지원 하지 않습니다. 지시문은 무시 됩니다.|지시문 프로세서가 지시문을 지원 하지 않을 때 발생 합니다. 메시지는 지시문 프로세서의 이름과 함께 잘못 된 지시문의 이름과 줄 번호를 제공 합니다.|지시문의 이름을 수정 합니다.|
|' @No__t_0 ' 파일에 대 한 include 지시문은 무한 루프를 발생 시킵니다.|순환 include 지시문이 지정 된 경우 표시 됩니다. 예를 들어 파일 A는 파일 A를 포함 하는 파일 B를 포함 합니다.|순환 include 지시문을 지정 하지 마십시오.|
|실행 중인 변환:|변환을 실행 하는 동안 생성 된 모든 오류 또는 경고에이 문자열을 앞에 표시 합니다.|해당 없음.|
|블록 내에 예기치 않은 시작 또는 끝 태그가 있습니다. 시작 태그와 끝 태그를 잘못 입력 하지 않았는지 확인 하 고 템플릿에 중첩 된 블록이 없습니다.|예기치 않은 \< # 또는 # > 있는 경우 표시 됩니다. 즉, 닫혀 있지 않은 다른 열린 태그 뒤에 \< #가 있거나, 앞에 닫히지 않은 열려 있는 태그가 없는 경우 # > 있습니다. 메시지는 일치 하지 않는 태그의 줄 번호를 제공 합니다.|일치 하지 않는 시작 또는 끝 태그를 제거 하거나 이스케이프 문자를 사용 합니다.|
|지시문에 잘못 된 형식이 지정 되었습니다. 지시문은 무시 됩니다. 지시문을 형식으로 지정 하십시오 `<#@ name [parametername="parametervalue"]*  #>`|지시문이 올바른 형식으로 지정 되지 않은 경우 파서에서 표시 합니다. 메시지는 잘못 된 지시문의 줄 번호를 제공 합니다.|모든 지시문이 `<#@ name [parametername="parametervalue"]*  #>` 폼에 있어야 합니다. 자세한 내용은 [T4 텍스트 템플릿 지시문](../modeling/t4-text-template-directives.md)을 참조 하세요.|
|등록 된 지시문 프로세서 ' {1} '에 대해 ' {0} ' 어셈블리를 로드 하지 못했습니다.<br /><br /> {2}|호스트에서 지시문 프로세서를 로드할 수 없을 때 발생 합니다. 이 메시지는 지시문 프로세서에 대해 제공 된 어셈블리와 지시문 프로세서의 이름을 식별 합니다.|지시문 프로세서가 올바르게 등록 되어 있고 어셈블리가 있는지를 확인해 두어야 합니다.|
|등록 된 지시문 프로세서 ' {2} '의 ' {1} ' 어셈블리에서 ' {0} ' 형식을 찾지 못했습니다.<br /><br /> {3}|지시문 프로세서 형식을 어셈블리에서 로드할 수 없을 때 발생 합니다. 메시지는 형식, 어셈블리 및 지시문 프로세서의 이름을 제공 합니다.|Vshost.exe는 레지스트리에서 지시문 프로세서 정보 (이름, 어셈블리 및 형식)를 찾습니다. 지시문 프로세서가 올바르게 등록 되어 있고 형식이 어셈블리에 있는지를 확인할 수 있습니다.|
|' @No__t_0 ' 어셈블리를 로드 하는 동안 문제가 발생 했습니다.|어셈블리를 로드 하는 데 문제가 있을 때 발생 합니다. 메시지는 어셈블리의 이름을 제공 합니다.|@No__t_0 @ #assembly # > 지시문 및 지시문 프로세서에서 로드할 어셈블리를 지정할 수 있습니다. 이 문자열 뒤에 오는 오류 메시지는 어셈블리 로드에 실패 한 이유에 대 한 추가 데이터를 제공 해야 합니다.|
|' @No__t_0 ' 지시문에 대 한 프로세서를 만들고 초기화 하는 동안 문제가 발생 했습니다. 프로세서 유형이 {0}입니다. 지시문은 무시 됩니다.|시스템에서 지시문 프로세서를 만들거나 초기화할 수 없는 경우에 발생 합니다. 메시지는 지시문의 이름과 줄 번호와 프로세서 형식을 제공 합니다.|올바른 지시문 프로세서를 사용 해야 하며, 지시문 프로세서에는 공용 기본 생성자가 있어야 합니다. 그렇지 않으면 디버그 옵션을 사용 하 여 지시문 프로세서의 Initialize () 메서드가 실패 하는 이유를 확인 합니다. 자세한 내용은 [텍스트 템플릿 문제 해결](../modeling/debugging-a-t4-text-template.md)을 참조 하세요.|
|' @No__t_0 ' 지시문을 처리 하는 동안 예외가 throw 되었습니다.|지시문 프로세서에서 지시문을 처리할 때 예외를 throw 할 때 발생 합니다.|지시문 프로세서에 대 한 매개 변수가 올바른지를 확인할 수 있습니다.|
|' @No__t_0 ' 어셈블리 참조를 확인 하는 동안 호스트에서 예외가 throw 되었습니다.|호스트에서 어셈블리 참조를 확인 하려고 할 때 예외를 throw 할 때 발생 합니다. 이 메시지는 어셈블리 참조 문자열을 제공 합니다.|어셈블리 참조는 \< @ #assembly # > 지시문 및 지시문 프로세서에서 제공 됩니다. Assembly 매개 변수에 제공 된 ' name ' 매개 변수가 올바른지를 지정 하십시오.|
|지시문에 대해 지원 되지 않는 {1} 값 ' {0} '을 지정 하려고 했습니다 {2}|지원 되지 않는 요구를 제공 하거나 인수를 제공 하는 경우 RequiresProvidesDirectiveProcessor (생성 된 모든 지시문 프로세서에서 파생 됨)에 의해 발생 합니다.|에서 제공 하는 name = ' value ' 쌍의 이름에가 필요 하며 매개 변수가 올바른지를 제공 해야 합니다.|
